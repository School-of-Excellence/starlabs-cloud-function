# Non-secret, per-project function config (Firebase CLI loads .env.<projectId>).
# DEVELOPMENT — starlabs-test.

# Appdata bucket of the openvidu-elastic-dev stack, used for recording egress and
# presigned playback. Auto-created by Terraform, so the random suffix is fixed at
# apply time. `bash scripts/oci/verify-oci-stack.sh dev` prints the current value.
OCI_RECORDING_BUCKET=openvidu-elastic-dev-appdata-d16985
